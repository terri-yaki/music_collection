<%= form_with(model: album, local: true) do |form| %>
  <% if album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--placeholder for preview of the input slot-->

  <div class="field form-group">
    <br>
    <%= form.label :Album_ID %>
    <%= form.text_field :alb_id, class:"form-control", placeholder:"Album ID" %>
  </div>
  <br>
  <div class="field form-group">
    <%= form.label :Album_Name %>
    <%= form.text_field :alb_name, class:"form-control", placeholder:"Album Name"  %>
  </div>
  <br>
  <div class="field form-group">
    <%= form.label :Release_year %>
    <%= form.text_field :alb_year, class:"form-control" , placeholder:"Release Year" %>
  </div>

  <div class="actions">
    <br>
    <%= form.submit %>
  </div>
<% end %>
